#pragma once
#include "Base.h"

BEGIN(Engine)

// 클라이언트에서 정의할 레벨들의 부모가 된다.
class ENGINE_DLL CLevel abstract : public CBase
{
protected:
	explicit CLevel(LPDIRECT3DDEVICE9 GraphicDevice); // 장치를 넘겨줘야 생성된다.
	virtual ~CLevel() = default;
public:
	virtual HRESULT NativeConstruct(); // 기본구성(초기화)
	virtual HRESULT Render();

protected:
	// 장치 보관
	LPDIRECT3DDEVICE9	mpGraphicDevice22;
private:
	uInt aaa1;
	uInt aaa2;
	uInt aaa3;

public:
	static void Create(); // 생성
	virtual void Free() override; // 장치를 지워준다.
};

END
